# No build step needed — this repo is a single static HTML file.
# GitHub Pages serves index.html directly from the main branch.
name: Disabled (static HTML — no build needed)
on:
  push:
    branches: ["_disabled"]
